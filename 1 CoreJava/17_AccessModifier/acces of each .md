Public marked method or variable -> can be used anywhere outside of a package too

private marked method or variabloes -> only accessible within the same class no where else

default marked variable -> only accessible within the same package not more

protected marked variable ->